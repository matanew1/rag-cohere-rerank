# chromadb has an optional peer dep on @google/generative-ai@^0.1.1 which conflicts
# with our @google/generative-ai@^0.21.0. The optional dep is only used by chromadb's
# built-in embedding functions, which we do not use.
legacy-peer-deps=true
